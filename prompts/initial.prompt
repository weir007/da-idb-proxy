我需要在keycloak admin api（https://www.keycloak.org/docs-api/latest/rest-api/openapi.json）之上封装自己的rest api，对外提供以下功能：

--- 租户管理：/tenants
1. 创建租户
实际对keycloak操作：
1) 创建realm，realm信息（如协议、接入参数，具体以keycloak要求为准）由用户填写
2) 在新建realm下创建一个默认client，命名为data-agent，协议为oidc，用户不感知
3) 在新建realm下创建一个角色tenant-admin，具有当前realm下所有操作权限

2. 删除租户：/
实际对keycloak操作：删除realm及其下关联的资源

3. 查看租户列表
实际对keycloak操作：返回realm信息列表，但要屏蔽内置realm（master）
 
# 后续接口操作都是在指定租户下执行的
 

--- ID Broker（IDP）管理
1. 导入SAML2.0 配置
实际对keycloak操作：导入SAML2.0元数据配置XML，返回解析后的配置json
注意keycloak admin api中定义有误，这里实际需要的是Multipart/Form-Data，而不是它宣称的text/json

2. 创建SAML2.0 Instance
实际对keycloak操作：根据用户输入配置创建ID Provider

--- 角色管理：
1. 添加角色
实际对keycloak操作：创建角色，可支持设置该role的 Attributes 映射规则

2. 查看角色列表

3. 查看角色详情

3. 更新角色
实际对keycloak操作：编辑指定角色信息，可支持设置该role的 Attributes 映射规则

4. 删除角色

--- 群组管理：
1. 查看群组列表
实际对keycloak操作：查看指定realm下的group列表

2. 查看群组信息
实际对keycloak操作：查看指定group信息，包括name、description、roles、members

3. 更新群组
实际对keycloak操作：更新指定group信息

4. 删除群组

--- 用户管理
1. 查看用户列表
实际对keycloak操作：查看指定realm下的用户列表

2. 查看用户信息
实际对keycloak操作：查看指定用户所属groups、roles


请使用python编写上述API，并提供测试文件

提示：
- 风格统一，建议优先使用fast api
- 要确保调用的是keycloak admin openai api，不要编造接口和参数，如果你没有关于keycloak admin openai api的记忆且无法在线读取，请务必告诉我，我来粘贴给你
- 调用keycloak api时，使用keycloak超管（admin）帐号
- 支持导出为openai api json文件
- 公共部分逻辑（如keycloak配置加载、token获取、HTTP Error处理）可以封装成统一的工具函数，我会根据需要调整内部逻辑，但该类函数不对外暴露
- 要能正确识别keycloak错误时的返回并优雅处理，不要出现keycloak返回400，而fast api返回 {code: 200, data {HTTP Status 400}}之类的滑稽错误
- 注意编码安全，特别是参数校验、敏感信息处理
